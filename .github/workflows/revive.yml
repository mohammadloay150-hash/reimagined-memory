name: Kaggle Zombie Bot Revival

on:
  schedule:
    - cron: '0 */10 * * *'
  workflow_dispatch:

jobs:
  revive-bot:
    runs-on: ubuntu-latest
    steps:
      - name: إعداد بيئة العمل
        uses: actions/checkout@v4

      - name: تجهيز بايثون
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: تثبيت مكتبة كاجل
        run: pip install kaggle

      - name: إعداد بيانات الدخول
        run: |
          mkdir -p ~/.kaggle
          # ملاحظة: تم وضع التوكن كاملاً مع KG_ لضمان الصلاحية
          echo '{"username":"mohammadloay","key":"KGAT_454b86bad813de576fa9533baaad2cad"}' > ~/.kaggle/kaggle.json
          chmod 600 ~/.kaggle/kaggle.json

      - name: سحب وإعادة تشغيل البوت (بدون GPU)
        run: |
          # 1. إنشاء المجلد وسحب ملف النوت بوك
          mkdir -p bot_code
          kaggle kernels pull mohammadloay/notebook954402994b -p bot_code
          
          # 2. إنشاء ملف الإعدادات يدوياً (مطابق تماماً لمتطلبات كاجل)
          echo '{
            "id": "mohammadloay/notebook954402994b",
            "title": "notebook954402994b",
            "code_file": "notebook954402994b.ipynb",
            "language": "python",
            "kernel_type": "notebook",
            "is_private": false,
            "enable_gpu": false,
            "enable_tpu": false,
            "enable_internet": true,
            "dataset_sources": [],
            "kernel_sources": [],
            "competition_sources": []
          }' > bot_code/kernel-metadata.json
          
          # 3. إضافة تغيير بسيط جداً داخل ملف النوت بوك لإجبار كاجل على تحديث الجلسة
          echo "" >> bot_code/notebook954402994b.ipynb
          
          # 4. إعادة الرفع لتشغيل الجلسة فوراً
          kaggle kernels push -p bot_code
